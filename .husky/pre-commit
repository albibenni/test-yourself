
set -eu

echo "Running pre-commit checks..."

# Type-check the frontend without emitting build files.
pnpm exec tsc --noEmit

# Biome checks both lint rules and formatting without changing or re-staging files.
pnpm biome check .

echo "Pre-commit checks passed!"
